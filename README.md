# URL Indexing Bot

Telegram bot for automated URL indexing with Google Search Console API.

## Features

- 🚀 Bulk URL indexing
- 🔐 Domain verification
- ⚡ Rate limiting
- 📊 Status tracking

## Setup

1. Clone repository
2. Install dependencies: `pip install -r requirements.txt`
3. Create `.env` file from `.env.example`
4. Add your bot token and credentials
5. Run: `python bot_integrated.py`

## Configuration

See `.env.example` for required environment variables.
